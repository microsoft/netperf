name: Custom Tasks

on:
  workflow_dispatch:

permissions: read-all

jobs:
  trigger-remote:
    name: Try running secnetperf remotely
    runs-on:
      - ubuntu-latest
    steps:
      uses: microsoft/netperf/.github/workflows/quic.yml@jackhe/make-secnetperf-callable
      with:
        ref: main
