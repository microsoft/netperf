name: Custom Tasks

on:
  workflow_dispatch:

permissions: write-all

jobs:
  test-job:
    runs-on:
      - self-hosted
      - 1ES.Pool=netperf-actual-boosted-winprerelease
    steps:
      - name: Hello
        run: echo "Hello, world!"
      - name: Is test signing enabled?
        run: |
          # Check if test signing is enabled
          bcdedit /enum | findstr "testsinging"
      